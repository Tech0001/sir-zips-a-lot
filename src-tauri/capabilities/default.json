{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Local desktop window and folder selection.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
